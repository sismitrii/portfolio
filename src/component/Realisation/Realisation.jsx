import SectionTitle from "../General/SectionTitle"

export default function Realisation(){
    return(
        <section id="realisations" className="w-screen flex justify-center items-center bg-bgBlue">
            <SectionTitle colored={false}>
                MES REALISATIONS
            </SectionTitle>
        </section>

    )
}